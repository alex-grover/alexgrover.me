{
  "name": "alexgrover.me",
  "description": "My personal website.",
  "private": true,
  "scripts": {
    "check:format": "prettier . --check --cache",
    "fix:format": "prettier . --write --cache"
  },
  "devDependencies": {
    "prettier": "3.5.2"
  }
}
